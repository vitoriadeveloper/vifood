spring.application.name=vifood
server.port=8081

# =========================
# ?? Configuração do PostgreSQL
# =========================
spring.datasource.url=jdbc:postgresql://localhost:5432/vifood
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# =========================
# ? Configuração do JPA / Hibernate
# =========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ? Garante que o data.sql rode após a criação das tabelas
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.sql.init.platform=postgresql

# ? Evita conflito com caches do Hibernate
spring.sql.init.continue-on-error=false

# =========================
# ? Configuração do H2 (opcional)
# =========================
#spring.datasource.url=jdbc:h2:mem:web_services
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console
